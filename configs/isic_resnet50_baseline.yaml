# Baseline ISIC ResNet-50 summary reproduction config.
name: isic_resnet50_baseline
alpha_values: [0.05, 0.10, 0.20]
methods: [standard_cp, aps, raps, mondrian_cp, frequency_grouped_cp]
paths:
  source_results_json: reproducibility_artifacts/isic_resnet50_framework_inputs/results.json
  framework_output_dir: assets/output/framework_runs/isic_resnet50_baseline
metadata:
  split_seed: 42
  status: summary_reproduction_from_release_artifact
